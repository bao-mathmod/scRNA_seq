# Data folder

Do not commit large raw datasets. Use this folder for small public teaching datasets only, or provide download scripts.

Suggested structure:

```text
data/
├── raw/
├── processed/
└── metadata/
```
